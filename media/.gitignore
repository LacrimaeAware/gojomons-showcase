# Binaries are ignored by default. Stills that are safe to publish get
# explicitly un-ignored below so the README can embed them by path.
# Video stays out unless converted to GIF (see README.md).
*
!.gitignore
!README.md
!StyleExploration.png
